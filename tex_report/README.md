# TeX Report

This folder contains the Homework 2 report draft.

Recommended compile command:

```bash
cd tex_report
export PATH=/data/yzj/texlive/2025/bin/x86_64-linux:$PATH
xelatex main.tex
xelatex main.tex
```

The report uses `ctexart`, so XeLaTeX is recommended for Chinese text.
On this machine, TeX Live is installed at `/data/yzj/texlive/2025/bin/x86_64-linux/`.

Before final submission, fill in:

- group member names, student IDs, and division of labor;
- public GitHub repository URL;
- model weight cloud-drive URL;
- SwanLab cloud screenshots if the instructor requires platform screenshots rather than exported training curves.
